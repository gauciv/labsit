<UserControl x:Class="LaboratorySitInSystem.Views.ScheduleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="DayOfWeekValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="sys:DayOfWeek"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Student Selector -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Schedule Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock Text="Student: " VerticalAlignment="Center" DockPanel.Dock="Left" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}"
                          DisplayMemberPath="FullName"/>
            </DockPanel>
        </StackPanel>

        <!-- Schedule DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Schedules}" SelectedItem="{Binding SelectedSchedule}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Subject" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Day" Binding="{Binding DayOfWeek}" Width="100"/>
                <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat=hh\\:mm}" Width="100"/>
                <DataGridTextColumn Header="End Time" Binding="{Binding EndTime, StringFormat=hh\\:mm}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Form and Actions -->
        <StackPanel Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="Subject Name"/>
                    <TextBox Text="{Binding EditSubjectName, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,4">
                    <TextBlock Text="Day of Week"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource DayOfWeekValues}}"
                              SelectedItem="{Binding EditDayOfWeek}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="Start Time (HH:mm)"/>
                    <TextBox Text="{Binding EditStartTime, StringFormat=hh\\:mm, UpdateSourceTrigger=LostFocus}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,4">
                    <TextBlock Text="End Time (HH:mm)"/>
                    <TextBox Text="{Binding EditEndTime, StringFormat=hh\\:mm, UpdateSourceTrigger=LostFocus}" Padding="4"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                <Button Content="Add" Command="{Binding AddCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Update" Command="{Binding UpdateCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Clear" Command="{Binding ClearFormCommand}" Padding="12,4"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" Margin="0,4,0,0" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
