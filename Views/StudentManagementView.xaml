<UserControl x:Class="LaboratorySitInSystem.Views.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Search -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Student Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DockPanel Margin="0,0,0,10">
                <Button Content="Search" Command="{Binding SearchCommand}" DockPanel.Dock="Right"
                        Padding="12,4" Margin="4,0,0,0"/>
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
            </DockPanel>
        </StackPanel>

        <!-- Student DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="120"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Course" Binding="{Binding Course}" Width="120"/>
                <DataGridTextColumn Header="Year" Binding="{Binding YearLevel}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Form and Actions -->
        <StackPanel Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="Student ID"/>
                    <TextBox Text="{Binding EditStudentId, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,4">
                    <TextBlock Text="First Name"/>
                    <TextBox Text="{Binding EditFirstName, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="Last Name"/>
                    <TextBox Text="{Binding EditLastName, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,4">
                    <TextBlock Text="Course"/>
                    <TextBox Text="{Binding EditCourse, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="Year Level"/>
                    <TextBox Text="{Binding EditYearLevel, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                <Button Content="Add" Command="{Binding AddCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Update" Command="{Binding UpdateCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,4" Margin="0,0,4,0"/>
                <Button Content="Clear" Command="{Binding ClearFormCommand}" Padding="12,4"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" Margin="0,4,0,0" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
