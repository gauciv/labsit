<UserControl x:Class="LaboratorySitInSystem.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="{Binding ApplicationName}" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,20" FontSize="14">
                <Run Text="Version "/><Run Text="{Binding Version, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" TextAlignment="Center"
                       Margin="0,0,0,20" FontSize="13"/>
            <TextBlock HorizontalAlignment="Center" FontSize="13">
                <Run Text="Developed by: "/><Run Text="{Binding Developer, Mode=OneWay}" FontWeight="SemiBold"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
